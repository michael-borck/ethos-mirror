[package]
name = "ethos-mirror"
version = "0.1.0"
description = "Teaching reflection and philosophy builder"
authors = ["Michael Borck"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
strip = true
lto = true
codegen-units = 1
